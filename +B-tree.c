#include <stdio.h>
#include <stdlib.h>
#include "database.h"
#include <string.h>

struct keys {struct keys* left; struct keys* right; int keyValue;};

int main(void){

	//char lolo[50] = ",lospollos:hermanos";
	// char lola = "p";
	//
	// printf("keyword is: %d\n", sizeof(lolo));
	//insert(lolo);
	readEverything();
	return 0;
}
